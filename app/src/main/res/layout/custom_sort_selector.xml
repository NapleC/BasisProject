<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/tv_sort_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/dp_2"
        android:layout_marginStart="@dimen/dp_4"
        android:ellipsize="end"
        android:gravity="center"
        android:maxLines="1"
        android:textColor="@color/selector_item_color"
        android:textSize="@dimen/sp_14" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/dp_4"
        android:layout_marginStart="@dimen/dp_2"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/iv_sort_rise"
            android:layout_width="20dp"
            android:layout_height="8dp"
            android:contentDescription="@string/title_mall"
            android:scaleType="centerCrop"
            app:srcCompat="@drawable/selector_sort_rise" />

        <ImageView
            android:id="@+id/iv_sort_drop"
            android:layout_width="20dp"
            android:layout_height="8dp"
            android:contentDescription="@string/title_mall"
            android:scaleType="centerCrop"
            app:srcCompat="@drawable/selector_sort_drop" />
    </LinearLayout>
</LinearLayout>
